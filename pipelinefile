pipeline {
agent any
  stages {
      stage ('Terraform init') { 
  steps {
   sh '''
   terraform init
   ''' 
   }
   }
   stage ('Terraform plan') { 
  steps {
   sh '''
   cd terraform-test/
   terraform plan -out=tfplan.out
   terraform show -json tfplan.out
   ''' 
   }
   }
}
}
